package com.qrdatmon.staff.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Centralized dimension values for consistent spacing across the app
 */
object Dimensions {
    // Status Bar & System UI Padding
    val statusBarPadding = 40.dp  // Padding for top of screen to avoid status bar overlap
    
    // Bottom Navigation
    val bottomNavHeight = 80.dp  // Material3 NavigationBar height
    val bottomNavPadding = 16.dp
    
    // Screen Padding
    val screenHorizontalPadding = 16.dp
    val screenVerticalPadding = 10.dp
    
    // Content Padding
    val contentBottomPadding = 96.dp  // Space for bottom navigation bar
}
