// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Plugins are managed via buildSrc convention plugins
    // No need to declare them here as they're already on classpath
}